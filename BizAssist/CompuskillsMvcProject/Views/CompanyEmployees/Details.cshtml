@model MvcProjectDbConn.CompanyEmployee
@using CompuskillsMvcProject.Models;
@{
    ErrorMessages error = new ErrorMessages();
    ViewBag.Title = "Details";
}
@{ if (ViewBag.error == true)
    {
        <p>@error.IdError()</p>
    }
    else if (ViewBag.clientError == true)
    {
        <p> @error.NullError("employee")</p>
    }
    else
    {



        <h2>Details</h2>

        <div>
            <h4>CompanyEmployees</h4>
            <hr />
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Name)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt>
                    @Html.DisplayNameFor(model => model.Company.CompanyName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Company.CompanyName)
                </dd>

                <dt>
                    @Html.DisplayNameFor(model => model.Employee.Email)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Employee.Email)
                </dd>

            </dl>
        </div>
        
           <span> @Html.ActionLink("Edit", "Edit", new { id = Model.id }) | </span>  
           <span> @Html.ActionLink("Delete", "Delete", new { id = Model.id })|</span>
          <div>  @Html.ActionLink("Back to List", "Index")</div>
        
    }
}
<style>
    span{
        margin:164px;
    }
</style>